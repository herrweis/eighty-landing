// ─── Scroll choreography ────────────────────────────────────────────────
// Layered on top of the static page. Implemented in the next step.
export function initMotion() {
  // TODO(next): GSAP + ScrollTrigger + Lenis three-act sequence.
}
